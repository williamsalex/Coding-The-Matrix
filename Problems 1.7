# Problem 1.7.1
def my_filter(A, B):
    multiples=[int(x/B) for x in A]
    filtered=[X*B for X in multiples]
    filtered2=[X for X in filtered if X==A]
    return(filtered)

# Problem 1.7.2
def my_lists(L):

# Problem 1.7.4
def numbers(L):
    for X in L:
        Y=(X+L[0])
    return(Y)
